# Conversational authoring support matrix

<!-- Generated by scripts/ai-support-matrix.mjs. Do not edit by hand; run `pnpm ai:matrix`. -->

Manifest version 2, baseline 1. Derived from `createQtiSchema()` with every interaction included.

Support is tracked per dimension:

- **Schema**: the node and attribute exist in the editor schema and survive HTML ⇄ ProseMirror.
- **Operation**: the agent may change it through a typed operation (`setAttributes` or `setVocabulary`). Identity and `class` are never edited directly.
- **Rendering**: `components` means the pinned `@qti-components` packages reference the token; `unverified` means it is standard vocabulary the editor preserves and exports but no component stylesheet was found for it.
- **Export**: shared vocabulary is standard QTI 3 and exports unchanged; attributes export through the item-export path.

Pinned components: `@citolab/qti-components@^9.1.0`, `@qti-components/associate-interaction@^1.1.6`, `@qti-components/base@^2.2.0`, `@qti-components/choice-interaction@^1.1.5`, `@qti-components/elements@^1.7.0`, `@qti-components/extended-text-interaction@^1.1.5`, `@qti-components/gap-match-interaction@^1.2.0`, `@qti-components/hottext-interaction@^1.2.0`, `@qti-components/inline-choice-interaction@^2.0.1`, `@qti-components/interactions-core@^2.1.2`, `@qti-components/item@^1.4.4`, `@qti-components/match-interaction@^2.0.0`, `@qti-components/order-interaction@^2.0.0`, `@qti-components/processing@^1.5.0`, `@qti-components/select-point-interaction@^1.1.5`, `@qti-components/text-entry-interaction@^1.1.6`, `@qti-components/theme@^2.2.0`, `@qti-components/transformers@^1.7.2`.

## Interactions

| Node | Tag | Attributes | Editable | Vocabulary groups | Rendering-unverified groups |
| --- | --- | --- | --- | --- | --- |
| `qtiChoiceInteraction` | `qti-choice-interaction` | 6 | 4 | 5 | 0 |
| `qtiExtendedTextInteraction` | `qti-extended-text-interaction` | 7 | 5 | 2 | 1 |
| `qtiGapMatchInteraction` | `qti-gap-match-interaction` | 6 | 4 | 1 | 0 |
| `qtiHottextInteraction` | `qti-hottext-interaction` | 6 | 4 | 1 | 0 |
| `qtiInlineChoiceInteraction` | `qti-inline-choice-interaction` | 6 | 4 | 1 | 0 |
| `qtiMatchInteraction` | `qti-match-interaction` | 5 | 3 | 2 | 0 |
| `qtiMatchInteractionTabular` | `qti-match-interaction` | 6 | 4 | 0 | 0 |
| `qtiOrderInteraction` | `qti-order-interaction` | 6 | 4 | 2 | 0 |
| `qtiSelectPointInteraction` | `qti-select-point-interaction` | 7 | 4 | 0 | 0 |
| `qtiTextEntryInteraction` | `qti-text-entry-interaction` | 6 | 4 | 1 | 0 |

### `qtiChoiceInteraction` (`qti-choice-interaction`)

Content: `qtiPrompt? qtiSimpleChoice+`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `maxChoices` | `max-choices` | integer ≥ 0 | ✓ | Maximum number of selections. 1 renders radio buttons; 0 means unlimited checkboxes; n > 1 allows at most n checkboxes. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |
| `shuffle` | `shuffle` | boolean | ✓ | Whether choices are presented in random order. Choices marked fixed keep their position. |

| Group | Selection | Tokens | Rendering | Export |
| --- | --- | --- | --- | --- |
| `labels` | single | `qti-labels-none`, `qti-labels-decimal`, `qti-labels-lower-alpha`, `qti-labels-upper-alpha` | components | standard |
| `labelsSuffix` | single | `qti-labels-suffix-none`, `qti-labels-suffix-period`, `qti-labels-suffix-parenthesis` | components | standard |
| `orientation` | single | `qti-orientation-vertical`, `qti-orientation-horizontal` | components | standard |
| `inputControlHidden` | boolean | `qti-input-control-hidden` | components | standard |
| `columns` | single | `qti-choices-stacking-1`, `qti-choices-stacking-2`, `qti-choices-stacking-3`, `qti-choices-stacking-4`, `qti-choices-stacking-5` | components | standard |

### `qtiExtendedTextInteraction` (`qti-extended-text-interaction`)

Content: `qtiPrompt?`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `expectedLength` | `expected-length` | integer \| null ≥ 0 | ✓ | Hint for the expected answer length in characters. |
| `expectedLines` | `expected-lines` | integer \| null ≥ 0 | ✓ | Hint for the expected number of lines of an extended answer. |
| `placeholderText` | `placeholder-text` | string \| null | ✓ | Placeholder shown in an empty input. |
| `patternMask` | `pattern-mask` | string \| null | ✓ | Regular expression the response must match. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |

| Group | Selection | Tokens | Rendering | Export |
| --- | --- | --- | --- | --- |
| `height` | single | `qti-height-lines-3`, `qti-height-lines-6`, `qti-height-lines-15` | components | standard |
| `counter` | single | `qti-counter-up`, `qti-counter-down` | **unverified** | standard |

### `qtiGapMatchInteraction` (`qti-gap-match-interaction`)

Content: `qtiPrompt? qtiGapText+ paragraph+`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `maxAssociations` | `max-associations` | integer ≥ 0 | ✓ | Maximum number of associations the candidate may make. 0 means unlimited. |
| `shuffle` | `shuffle` | boolean | ✓ | Whether choices are presented in random order. Choices marked fixed keep their position. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |

| Group | Selection | Tokens | Rendering | Export |
| --- | --- | --- | --- | --- |
| `choicesPlacement` | single | `qti-choices-top`, `qti-choices-bottom`, `qti-choices-left`, `qti-choices-right` | components | standard |

### `qtiHottextInteraction` (`qti-hottext-interaction`)

Content: `paragraph+`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `maxChoices` | `max-choices` | integer ≥ 0 | ✓ | Maximum number of selections. 1 renders radio buttons; 0 means unlimited checkboxes; n > 1 allows at most n checkboxes. |
| `minChoices` | `min-choices` | integer ≥ 0 | ✓ | Minimum number of selections required. 0 means no minimum. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |

| Group | Selection | Tokens | Rendering | Export |
| --- | --- | --- | --- | --- |
| `unselectedHidden` | boolean | `qti-unselected-hidden` | components | standard |

### `qtiInlineChoiceInteraction` (`qti-inline-choice-interaction`)

Content: `qtiInlineChoice+`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `shuffle` | `shuffle` | boolean | ✓ | Whether choices are presented in random order. Choices marked fixed keep their position. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |
| `dataPrompt` | `data-prompt` | string \| null | ✓ | Prompt shown as the unselected option of an inline choice. |

| Group | Selection | Tokens | Rendering | Export |
| --- | --- | --- | --- | --- |
| `inputWidth` | single | `qti-input-width-1`, `qti-input-width-2`, `qti-input-width-3`, `qti-input-width-4`, `qti-input-width-6`, `qti-input-width-10`, `qti-input-width-15`, `qti-input-width-20`, `qti-input-width-25`, `qti-input-width-30`, `qti-input-width-35`, `qti-input-width-40`, `qti-input-width-45`, `qti-input-width-50`, `qti-input-width-72` | components | standard |

### `qtiMatchInteraction` (`qti-match-interaction`)

Content: `qtiPrompt? qtiSimpleMatchSet{2}`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `shuffle` | `shuffle` | boolean | ✓ | Whether choices are presented in random order. Choices marked fixed keep their position. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |

| Group | Selection | Tokens | Rendering | Export |
| --- | --- | --- | --- | --- |
| `presentation` | boolean | `qti-match-tabular` | components | standard |
| `choicesPlacement` | single | `qti-choices-top`, `qti-choices-bottom`, `qti-choices-left`, `qti-choices-right` | components | standard |

### `qtiMatchInteractionTabular` (`qti-match-interaction`)

Content: `qtiPrompt? qtiSimpleMatchSet{2}`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `shuffle` | `shuffle` | boolean | ✓ | Whether choices are presented in random order. Choices marked fixed keep their position. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |
| `dataFirstColumnHeader` | `data-first-column-header` | string \| null | ✓ | Header of the first column of a tabular match interaction. |

_No shared vocabulary for this node._

### `qtiOrderInteraction` (`qti-order-interaction`)

Content: `qtiPrompt? qtiSimpleChoice+`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `shuffle` | `shuffle` | boolean | ✓ | Whether choices are presented in random order. Choices marked fixed keep their position. |
| `orientation` | `orientation` | enum \| null (vertical, horizontal) | ✓ | Layout direction of an order interaction. Prefer the orientation vocabulary group; this attribute is the legacy form. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |

| Group | Selection | Tokens | Rendering | Export |
| --- | --- | --- | --- | --- |
| `orientation` | single | `qti-orientation-vertical`, `qti-orientation-horizontal` | components | standard |
| `choicesPlacement` | single | `qti-choices-top`, `qti-choices-bottom`, `qti-choices-left`, `qti-choices-right` | components | standard |

### `qtiSelectPointInteraction` (`qti-select-point-interaction`)

Content: `qtiPrompt? imgSelectPoint`

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `maxChoices` | `max-choices` | integer ≥ 0 | ✓ | Maximum number of selections. 1 renders radio buttons; 0 means unlimited checkboxes; n > 1 allows at most n checkboxes. |
| `minChoices` | `min-choices` | integer ≥ 0 | ✓ | Minimum number of selections required. 0 means no minimum. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `areaMappings` | `area-mappings` | string \| null | via editor | JSON list of scored areas of a select-point interaction. Edited through the image, not by hand. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |

_No shared vocabulary for this node._

### `qtiTextEntryInteraction` (`qti-text-entry-interaction`)

Content: ``

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `responseIdentifier` | `response-identifier` | string \| null | via editor | Identifier of the response variable. Unique per item; managed by the editor. |
| `correctResponse` | `correct-response` | response \| null | ✓ | The correct answer(s): comma-separated identifiers for choices, answer strings for text entry, "source target" pairs for matching and gap match, ordered identifiers for ordering. |
| `caseSensitive` | `case-sensitive` | boolean | ✓ | Whether text answers are compared case-sensitively. |
| `class` | `class` | string \| null | via vocabulary | Space-separated class tokens. Change shared vocabulary with setVocabulary, never by rewriting this value. |
| `placeholderText` | `placeholder-text` | string \| null | ✓ | Placeholder shown in an empty input. |
| `score` | `score` | number ≥ 0 | ✓ | Points awarded for a fully correct response. Defaults to 1. |

| Group | Selection | Tokens | Rendering | Export |
| --- | --- | --- | --- | --- |
| `inputWidth` | single | `qti-input-width-1`, `qti-input-width-2`, `qti-input-width-3`, `qti-input-width-4`, `qti-input-width-6`, `qti-input-width-10`, `qti-input-width-15`, `qti-input-width-20`, `qti-input-width-25`, `qti-input-width-30`, `qti-input-width-35`, `qti-input-width-40`, `qti-input-width-45`, `qti-input-width-50`, `qti-input-width-72` | components | standard |

## Child nodes with attributes

### `qtiGap` (`qti-gap`)

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `identifier` | `identifier` | string \| null | via editor | Identifier of a choice, gap or hottext. Unique within its interaction; referenced by correctResponse. |
| `matchMax` | `match-max` | integer ≥ 0 | ✓ | How many times this choice may be used in associations. 0 means unlimited. |

### `qtiGapText` (`qti-gap-text`)

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `identifier` | `identifier` | string \| null | via editor | Identifier of a choice, gap or hottext. Unique within its interaction; referenced by correctResponse. |
| `matchMax` | `match-max` | integer ≥ 0 | ✓ | How many times this choice may be used in associations. 0 means unlimited. |

### `qtiHottext` (`qti-hottext`)

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `identifier` | `identifier` | string \| null | via editor | Identifier of a choice, gap or hottext. Unique within its interaction; referenced by correctResponse. |

### `qtiInlineChoice` (`qti-inline-choice`)

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `identifier` | `identifier` | string \| null | via editor | Identifier of a choice, gap or hottext. Unique within its interaction; referenced by correctResponse. |

### `qtiRubricBlock` (`qti-rubric-block`)

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `use` | `use` | string | ✓ | _derived from schema default_ |
| `view` | `view` | string | ✓ | _derived from schema default_ |

### `qtiSimpleAssociableChoice` (`qti-simple-associable-choice`)

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `identifier` | `identifier` | string \| null | via editor | Identifier of a choice, gap or hottext. Unique within its interaction; referenced by correctResponse. |
| `matchMax` | `match-max` | integer ≥ 0 | ✓ | How many times this choice may be used in associations. 0 means unlimited. |
| `matchMin` | `match-min` | integer ≥ 0 | ✓ | How many times this choice must be used at least. |
| `fixed` | `fixed` | boolean | ✓ | Keep this choice in place when the interaction shuffles. |

### `qtiSimpleChoice` (`qti-simple-choice`)

| Attribute | HTML | Type | Operation | Meaning |
| --- | --- | --- | --- | --- |
| `identifier` | `identifier` | string \| null | via editor | Identifier of a choice, gap or hottext. Unique within its interaction; referenced by correctResponse. |
| `fixed` | `fixed` | boolean | ✓ | Keep this choice in place when the interaction shuffles. |

## Intentional gaps and limitations

- **Not in the schema**: hotspot, graphic gap match, associate, graphic order, slider, drawing, upload, media and custom interactions. The agent cannot create or convert to them; the manifest does not list them.
- **Select point**: `areaMappings`, image source and size are edited through the image tooling, not by the agent. Only `maxChoices`, `minChoices`, `correctResponse` and `score` are operations.
- **Structural child edits** (adding or removing choices, gaps, hottexts, match sets) go through `insert` / `replace` of validated HTML, not through attribute operations. Identifiers on replacements must be preserved.
- **General-purpose shared vocabulary** (layout rows and columns, alignment, writing mode, `qti-keyword-emphasis`) is content, not an attribute operation; it is emitted inside generated HTML and validated through the schema.
- **Rendering caveats**: `qti-counter-up` / `qti-counter-down` are preserved and exported but no pinned component styles them. `orientation` on order interactions has both an attribute and a vocabulary group; the vocabulary group is preferred and the attribute is dropped from HTML when the class is present.
- **Two match representations**: `qtiMatchInteraction` and `qtiMatchInteractionTabular` both serialize to `qti-match-interaction`; the tabular node is the editor's table form and carries `dataFirstColumnHeader`.
