---
title: Integration Overview
description: How QTI Editor integrates into a TypeScript application.
slug: docs/frameworks/overview
---

QTI Editor is a composable authoring kit built on [ProseMirror](https://prosemirror.net/). It ships as a set of focused packages that add QTI interaction types, commands, and serialization on top of a standard ProseMirror instance. There is no proprietary editor core to embed — your application owns the DOM and lifecycle; the QTI packages slot in as descriptors, plugins, and node specs.

The canonical integration shape is [`qti-prosemirror-item`](https://github.com/Citolab/qti-editor/tree/main/apps/qti-prosemirror-item), a minimal pure-ProseMirror QTI roundtrip editor. The [TypeScript Integration](/docs/frameworks/vanilla/) guide walks through that shape file by file.

## The layer model

| Layer | What it is | Required |
|---|---|---|
| ProseMirror | Document model, view, and transaction system | Yes |
| `@citolab/prose-qti` | Descriptor registry, all QTI interactions, item roundtrip | Yes |
| `@citolab/prose-extensions` | Generic ProseMirror extensions (block selection, node attrs sync, etc.) | Optional |

:::note
QTI Editor is a composable kit — it provides the QTI building blocks that slot into an existing ProseMirror instance. It does not ship integration packages for specific frameworks or third-party libraries, and dedicated integration support is outside its scope. The guide illustrates one way to wire things together; how you integrate the kit into your application is up to you.
:::

## What you assemble

The boilerplate splits its editor across three files:

- **`schema.ts`** — the document topology. Each interaction's `qti*InteractionNodeSpec` is spread into one literal `Schema` with explicit `content` and `group` rules.
- **`prosemirror-qti.ts`** — the QTI integration layer. Imports each `*InteractionDescriptor` explicitly, derives the `qtiPlugins` array and `editableAttrs` allowlist from them, and exposes the `importItemFromUrl` / `exportItemXml` roundtrip helpers from `@citolab/prose-qti/item-roundtrip`.
- **`main.ts`** — the composition root. `new EditorView(...)` + `EditorState.create({ doc, plugins })`, with `qtiPlugins` ordered before the list-split and `keymap(baseKeymap)` so the QTI Enter/Backspace overrides win and unhandled keys fall through.

## qti-components

QTI Editor depends on [`@qti-components/*`](https://github.com/Citolab/qti-components) for the underlying QTI web components that render and encapsulate interaction behavior at runtime. `@citolab/prose-qti` pulls in the required `@qti-components` dependencies automatically.

## Guide

- [TypeScript Integration](/docs/frameworks/vanilla/) — the boilerplate, file by file
