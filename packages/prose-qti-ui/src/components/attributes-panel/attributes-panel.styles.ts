import { css, type CSSResultGroup } from 'lit';

const styles: CSSResultGroup = [
  css`
    :host {
      display: block;
      font: inherit;
      color: inherit;
    }
  `,
];

export default styles;
