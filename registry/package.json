{
  "name": "@qti-editor/registry",
  "version": "0.1.0",
  "description": "Component registry for QTI Editor - shadcn-compatible registry items",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build-registry.js",
    "serve": "npx serve public -p 4100 --cors"
  },
  "dependencies": {
    "@lit/context": "^1.1.0",
    "@qti-editor/core": "workspace:*",
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
